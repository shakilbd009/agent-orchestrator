# evals/integration/brd-02-platform-orchestration-health-integration.md

**Project:** agent-orchestrator  
**BRD:** BRD-02 — Platform-Native Orchestration Pipeline  
**Type:** Integration test contracts  
**Owner:** qa  
**Status:** 🔴 **Failing** (before implementation)

---

## Integration Test: GET /live returns 200 when service is running

### Setup
- Backend server running with `platform-orchestration=true`

### Steps
1. GET /live

### Assertions
- Response `200 OK`
- Service is responsive (per FR-02-021 health endpoint definition)

---

## Integration Test: GET /ready returns 200 when all subsystems available

### Setup
- All subsystems up: DB, audit log, webhook queue, SSE fanout

### Steps
1. GET /ready

### Assertions
- Response `200 OK`
- All required subsystems reported as available

---

## Integration Test: GET /ready fails when orchestration storage is unavailable

### Setup
- Simulate DB connection failure or corruption

### Steps
1. GET /ready

### Assertions
- Response `503 Service Unavailable`
- Failing subsystem identified in response body

---

## Integration Test: GET /ready fails when audit event persistence is unavailable

### Setup
- Storage reachable but audit log writes failing

### Steps
1. GET /ready

### Assertions
- Response `503 Service Unavailable`
- Audit event persistence identified as failing

---

## Integration Test: GET /ready fails when webhook enqueueing is unavailable

### Setup
- Webhook queue subsystem unavailable

### Steps
1. GET /ready

### Assertions
- Response `503 Service Unavailable`
- Webhook queue identified as failing

---

## Integration Test: GET /ready does NOT fail when webhook receiver is down

### Setup
- Webhook consumer URL returning 500 (receiver outage)
- Webhook queue (enqueue capability) still available

### Steps
1. GET /ready

### Assertions
- Response `200 OK` (webhook receiver outages don't make platform unready per ADR-02-004)
- Platform remains available for reads and mutations

---

## Integration Test: Health check does not block or mutate state

### Setup
- Service running with active orchestration load

### Steps
1. GET /live (multiple times rapidly)
2. GET /ready (multiple times rapidly)

### Assertions
- Health checks complete without side effects
- No audit events generated by health checks themselves
- No state mutation from health check endpoints

---

*End of health/readiness integration tests*